version: '3.8'

services:
  server:
    build: ./server
    ports:
      - "3001:3001"
    restart: always

  client:
    build: ./client
    ports:
      - "80:80"
    depends_on:
      - server
    restart: always